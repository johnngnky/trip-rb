image: node

before_script:
  - node --version
  - npm --version
  - npm ci || npm install

make:
  script:
    - npm run make
  artifacts:
    paths:
      - static/

pages:
  stage: deploy
  script:
    - npm run make
  environment:
    name: Production
    url: https://react-bootstrap-example.tripetto.com/
  artifacts:
    paths:
      - static
  only:
    - master
